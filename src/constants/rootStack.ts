import { createStackNavigator } from '@react-navigation/stack'
import { type RootStackParamList } from '../interface/roots'

const RootStack = createStackNavigator<RootStackParamList>()
export default RootStack
